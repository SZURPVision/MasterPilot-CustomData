syntax = "proto3";

option csharp_namespace = "MasterPilot.Communicator.ProtoMessages.Control";

// 速度线性量
message SpeedFactor {
    // X轴速度比例 (0~255, 255为满速, 0为静止, 无负数)
    uint32 x = 1;
    
    // Y轴速度比例 (0~255, 255为满速, 0为静止, 无负数)
    uint32 y = 2;
}
